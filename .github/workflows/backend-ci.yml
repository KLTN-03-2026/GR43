name: Backend CI/CD

on:
  push:
    branches:
      - main
      - cicd
    paths:
      - "backend/**"
      - "k8s/**"
      - ".github/workflows/**"

permissions:
  contents: read
  packages: write

jobs:
  build-and-deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0

      - name: Restore
        run: dotnet restore backend

      - name: Build
        run: dotnet build backend --configuration Release

      - name: Publish
        run: dotnet publish backend -c Release -o publish

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/datn2026-stt43-backend:latest

      - name: Setup kubectl
        uses: azure/setup-kubectl@v3

      - name: Set Kubernetes Context
        uses: azure/k8s-set-context@v3
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: Deploy to Kubernetes
        env:
          MONGODB_CONNECTION_STRING: ${{ secrets.MONGODB_CONNECTION_STRING }}
          REDIS_CONNECTION_STRING: ${{ secrets.REDIS_CONNECTION_STRING }}
        run: |
          envsubst < k8s/secret.yml > k8s/secret_ready.yml

          kubectl apply -f k8s/redis.yml
          kubectl apply -f k8s/configmap.yml
          kubectl apply -f k8s/secret_ready.yml
          kubectl apply -f k8s/service.yml
          kubectl apply -f k8s/deployments.yml

          kubectl rollout restart deployment/datn2026-stt43-backend
