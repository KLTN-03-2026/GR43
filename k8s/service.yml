apiVersion: v1
kind: Service
metadata:
  name: datn2026-stt43-backend-service
  namespace: datn2026-stt43
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 5050
  type: NodePort
